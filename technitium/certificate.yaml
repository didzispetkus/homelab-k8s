apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: technitium-certificate
  namespace: technitium
spec:
  secretName: technitium-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - technitium.petkus.id.lv