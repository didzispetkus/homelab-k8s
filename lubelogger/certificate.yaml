apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lubelog-certificate
  namespace: lubelog
spec:
  secretName: lubelog-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - lubelog.petkus.id.lv