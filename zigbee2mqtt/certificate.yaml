apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zigbee2mqtt-certificate
  namespace: zigbee2mqtt
spec:
  secretName: zigbee2mqtt-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - zigbee2mqtt.petkus.id.lv